<h1>Update the client</h1>
	
<%= render 'form' %>

<%= link_to "Cancel and return to clients home", clients_path%>
<%= link_to "Delete Client", client_path(@client), data: { "turbo_method": :delete, "turbo-confirm": "Are you sure?"} %>